x = int(input('Enter the number: '))  # รับเลขจากผู้ใช้
i = 0  # เริ่มต้นที่ 0 เพราะตารางคูณเริ่มต้นที่ 0
while i < 10:  # ทำซ้ำ 10 ครั้ง
    print(f'{i} x {x} = {i * x}')  # แสดงผลการคูณ
    i += 1  # เพิ่มค่า i ทีละ 1
